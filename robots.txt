User-agent: *
# Allow the main site and static assets needed for rendering (CSS/JS/images)
Allow: /

# Disallow internal or sensitive directories that should not be indexed
Disallow: /logs/
Disallow: /shared_storage/
Disallow: /functions/

# Sitemap location
Sitemap: https://app.meteoride.cc/sitemap.xml

# Note: Do NOT disallow /scripts/ or /style.css — search engines need access to JS/CSS
# to render Single Page Apps correctly. Only disallow directories that contain
# source files, logs or user-uploaded content you don't want crawled.
